const express = require('express');
const router = express.Router();
const upload = require('../config/multerConfig'); // Importer Multer
const { updatePhoto } = require('../controllers/etudiantController'); // Importer le contrôleur

// Route pour mettre à jour la photo de profil
router.post('/update-photo', upload.single('photo'), updatePhoto);

module.exports = router;
