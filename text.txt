useEffect(() => {
  // Appel API pour récupérer la couverture de profil du groupePartage
  api
    .get(`/partageGroupe/couverture/${groupId}`)
    .then((response) => {
      setcouverture(response.data.groupe.couverture);  // Correction ici
    })
    .catch((error) => {
      console.error("Erreur lors de la récupération de la couverture:", error);
    });
}, [groupId]);
