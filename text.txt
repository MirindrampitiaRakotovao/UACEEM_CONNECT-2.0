
const bodyParser = require('body-parser'); // Si tu utilises body-parser


// Pour les requêtes JSON
app.use(express.json()); // Permet de parser les requêtes avec un corps JSON

// Pour les données encodées dans l'URL (formulaires)
app.use(express.urlencoded({ extended: true }));

// Si tu utilises body-parser (optionnel)
app.use(bodyParser.json()); // Ancienne façon pour les requêtes JSON
