<li
              className={`relative flex flex-col items-center cursor-pointer ${currentPath === '/homeAdmin' ? 'w-16' : 'w-12'}`}  // Ajout de flex et centrer
              onClick={() => navigate('/homeAdmin')}
            >
              <Home size={25} className={`${currentPath === '/homeAdmin' ? 'text-blue-600' : 'dark:text-white'}`} />
              {currentPath === '/homeAdmin' && (
                <span className="absolute bottom-0 left-0 right-0 mx-auto h-[6px] w-[40%] bg-blue-600 rounded-full"></span> // Barre plus large et centrée
              )}
            </li>